char ** mytoc(char *str, char delim);			/* Tokenize using delimiter */
char * uinteraction();
void tread();														/* reads stdin */
int ischar(char c, char delim);
int tcount(char *str, int start, char delim);	/* counts the number or char using the delimiter*/

void strcopy(char *dest, char *src, char delim);
